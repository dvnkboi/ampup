/*

$(":button").click(function(){
    setTimeout(function(){
        //console.log("did it boss");
        $(':button').prop('disabled', true);
        $(':button').blur();
        $(':button').prop('disabled', false);
        $(':button').prop('active', false);
    },200);
    switch($(this).attr('id')) {
      case "panelbtn":
        window.location = "#Info_Panel";
        break;
      default:
    }
});
*/